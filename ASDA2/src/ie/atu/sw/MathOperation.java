package ie.atu.sw;

import java.util.Arrays;

//MathOperation interface does the math jobs 
public interface MathOperation {
 // Performing operations on an array of Numbers.
 double operate(Number[] numbers);
}